<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      line-height: 1.6;
      color: #333;
      background-color: #f5f5f5;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      background-color: white;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }
    .header {
      background: linear-gradient(135deg, #e63946 0%, #d62828 100%);
      color: white;
      padding: 44px 24px 38px;
      text-align: center;
      animation: slideIn 0.5s ease-out;
    }
    .header h1 {
      margin: 0;
      font-size: 28px;
      font-weight: 700;
      letter-spacing: 0.5px;
    }
    .header p {
      margin: 10px 0 0 0;
      font-size: 14px;
      opacity: 0.95;
    }
    .content {
      padding: 32px 24px;
    }
    .lead {
      margin: 0 0 18px 0;
      font-size: 15px;
      color: #4b5563;
    }
    .section {
      margin-bottom: 24px;
      padding: 20px;
      background-color: #f9f9f9;
      border-left: 4px solid #e63946;
      border-radius: 4px;
      animation: fadeIn 0.6s ease-out;
    }
    .section h3 {
      margin: 0 0 15px 0;
      color: #e63946;
      font-size: 16px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    .section-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 15px;
    }
    .info-item {
      margin-bottom: 12px;
    }
    .info-label {
      font-size: 12px;
      color: #888;
      text-transform: uppercase;
      font-weight: 600;
      letter-spacing: 0.5px;
      margin-bottom: 3px;
    }
    .info-value {
      font-size: 16px;
      font-weight: 500;
      color: #333;
    }
    .goals-box {
      background-color: white;
      padding: 15px;
      border-radius: 4px;
      border: 1px solid #e0e0e0;
      font-style: italic;
      color: #666;
      line-height: 1.6;
    }
    .cta-section {
      text-align: center;
      margin: 28px 0 20px;
      padding: 22px;
      background: linear-gradient(135deg, #f0f0f0 0%, #ffffff 100%);
      border-radius: 6px;
    }
    .cta-button {
      display: inline-block;
      background: linear-gradient(135deg, #e63946 0%, #d62828 100%);
      color: white;
      padding: 12px 30px;
      text-decoration: none;
      border-radius: 4px;
      font-weight: 600;
      font-size: 14px;
      transition: transform 0.2s;
    }
    .cta-button:hover {
      transform: scale(1.05);
    }
    .footer {
      background-color: #f9f9f9;
      border-top: 1px solid #e0e0e0;
      padding: 22px 20px;
      text-align: center;
      font-size: 12px;
      color: #888;
    }
    .divider {
      border: 0;
      height: 1px;
      background: #e0e0e0;
      margin: 20px 0;
    }
    @keyframes slideIn {
      from {
        transform: translateY(-10px);
        opacity: 0;
      }
      to {
        transform: translateY(0);
        opacity: 1;
      }
    }
    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }
    @media (max-width: 600px) {
      .section-grid {
        grid-template-columns: 1fr;
      }
      .header h1 {
        font-size: 22px;
      }
      .content {
        padding: 20px 15px;
      }
      .section {
        padding: 15px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Header -->
    <div class="header">
      <h1>📅 New Booking Request</h1>
      <p>You have a new training session booking</p>
    </div>

    <!-- Content -->
    <div class="content">
      <p class="lead">
        A new booking request has been submitted. Please review the details and confirm or decline as soon as possible.
      </p>
      <!-- Client Information Section -->
      <div class="section">
        <h3>👤 Client Information</h3>
        <div class="section-grid">
          <div class="info-item">
            <div class="info-label">Client Name</div>
            <div class="info-value"><%= @booking[:user_name] || "N/A" %></div>
          </div>
          <div class="info-item">
            <div class="info-label">Email</div>
            <div class="info-value">
              <a href="mailto:<%= @booking[:user_email] %>" style="color: #e63946; text-decoration: none;">
                <%= @booking[:user_email] || "N/A" %>
              </a>
            </div>
          </div>
          <div class="info-item">
            <div class="info-label">Phone Number</div>
            <div class="info-value">
              <a href="tel:<%= @booking[:user_phone] %>" style="color: #e63946; text-decoration: none;">
                <%= @booking[:user_phone] || "N/A" %>
              </a>
            </div>
          </div>
        </div>
      </div>

      <!-- Session Details Section -->
      <div class="section">
        <h3>⏰ Session Details</h3>
        <div class="section-grid">
          <div class="info-item">
            <div class="info-label">Preferred Date</div>
            <div class="info-value"><%= @booking[:preferred_date] || "N/A" %></div>
          </div>
          <div class="info-item">
            <div class="info-label">Preferred Time</div>
            <div class="info-value"><%= @booking[:preferred_time] || "N/A" %></div>
          </div>
        </div>
      </div>

      <!-- Client Goals/Requirements Section -->
      <% if @booking[:goals_message].present? %>
        <div class="section">
          <h3>🎯 Goals & Requirements</h3>
          <div class="goals-box">
            <%= simple_format(@booking[:goals_message]) %>
          </div>
        </div>
      <% end %>

      <!-- Call to Action -->
      <div class="cta-section">
        <p style="margin: 0 0 15px 0; color: #666;">
          Please review this booking request and confirm your availability.
        </p>
        <a href="https://fitelitegym.com/trainer/bookings" class="cta-button">View in Dashboard</a>
      </div>

      <hr class="divider">

      <!-- Additional Info -->
      <p style="text-align: center; color: #888; font-size: 13px; margin: 20px 0;">
        <strong>Next Steps:</strong> Please confirm or decline this booking request as soon as possible to keep your client informed.
      </p>
    </div>

    <!-- Footer -->
    <div class="footer">
      <p style="margin: 0;">
        This is an automated notification from FitElite Gym<br>
        © <%= Date.today.year %> FitElite Gym. All rights reserved.
      </p>
    </div>
  </div>
</body>
</html>
