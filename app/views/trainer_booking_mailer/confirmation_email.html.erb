<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap');
    
    body { 
      font-family: 'Poppins', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
      padding: 20px;
      color: #1f2937;
      line-height: 1.6;
      min-height: 100vh;
    }
    
    .container { 
      max-width: 600px;
      margin: 0 auto;
      background: #ffffff;
      border-radius: 20px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      overflow: hidden;
      border: 1px solid rgba(255, 255, 255, 0.1);
      animation: slideIn 0.6s ease-out;
    }
    
    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    .header {
      background: linear-gradient(135deg, #e63946 0%, #d62828 100%);
      color: #fff;
      padding: 50px 20px 40px;
      text-align: center;
      position: relative;
      overflow: hidden;
    }
    
    .header::before {
      content: '';
      position: absolute;
      top: -50%;
      right: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle, rgba(255,255,255,0.15) 0%, transparent 70%);
      animation: pulse 4s ease-in-out infinite;
    }
    
    @keyframes pulse {
      0%, 100% { transform: scale(1); opacity: 0.5; }
      50% { transform: scale(1.1); opacity: 0.8; }
    }
    
    .header-content {
      position: relative;
      z-index: 1;
    }
    
    .header h1 {
      margin: 0;
      font-size: 32px;
      font-weight: 700;
      letter-spacing: -0.5px;
      text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
      animation: fadeInDown 0.8s ease-out;
    }
    
    .header-subtitle {
      font-size: 14px;
      margin-top: 8px;
      opacity: 0.95;
      font-weight: 500;
      animation: fadeInDown 0.8s ease-out 0.1s backwards;
    }
    
    @keyframes fadeInDown {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    .checkmark {
      display: inline-block;
      width: 32px;
      height: 32px;
      margin-left: 10px;
      animation: scaleIn 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    }
    
    @keyframes scaleIn {
      from {
        opacity: 0;
        transform: scale(0);
      }
      to {
        opacity: 1;
        transform: scale(1);
      }
    }
    
    .content {
      padding: 40px 30px;
      background: #ffffff;
    }
    
    .greeting {
      font-size: 18px;
      font-weight: 600;
      color: #1f2937;
      margin-bottom: 10px;
      animation: fadeIn 0.8s ease-out 0.2s backwards;
    }
    
    .intro-text {
      font-size: 14px;
      color: #6b7280;
      margin-bottom: 30px;
      line-height: 1.8;
      animation: fadeIn 0.8s ease-out 0.3s backwards;
    }
    
    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }
    
    .detail-card {
      background: #f8f9fa;
      border: 1px solid #dee2e6;
      border-radius: 12px;
      padding: 20px;
      margin: 20px 0;
      transition: all 0.3s ease;
      animation: slideInUp 0.8s ease-out 0.4s backwards;
    }
    
    @keyframes slideInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    .detail-card:hover {
      border-color: #e63946;
      box-shadow: 0 6px 16px rgba(230, 57, 70, 0.12);
      transform: translateY(-2px);
    }
    
    .detail-card h3 {
      color: #e63946;
      margin: 0 0 16px 0;
      font-size: 15px;
      font-weight: 700;
      display: flex;
      align-items: center;
      gap: 8px;
    }
    
    .icon {
      width: 20px;
      height: 20px;
      display: inline-block;
      flex-shrink: 0;
    }
    
    .detail-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 10px 0;
      border-bottom: 1px solid #e9ecef;
    }
    
    .detail-row:last-child {
      border-bottom: none;
    }
    
    .detail-label {
      color: #868e96;
      font-weight: 500;
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 0.3px;
    }
    
    .detail-value {
      color: #212529;
      font-weight: 600;
      font-size: 14px;
    }
    
    .cta-section {
      margin: 30px 0;
      text-align: center;
      animation: fadeIn 0.8s ease-out 0.6s backwards;
    }
    
    .cta-button {
      display: inline-block;
      background: linear-gradient(135deg, #e63946 0%, #d62828 100%);
      color: white;
      padding: 13px 38px;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 600;
      font-size: 14px;
      transition: all 0.3s ease;
      box-shadow: 0 4px 12px rgba(230, 57, 70, 0.25);
      border: none;
      cursor: pointer;
    }
    
    .cta-button:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(230, 57, 70, 0.35);
    }
    
    .info-box {
      background: #e7f5ff;
      border-left: 4px solid #e63946;
      padding: 16px;
      border-radius: 6px;
      margin: 20px 0;
      font-size: 13px;
      color: #1f2937;
      line-height: 1.7;
      animation: slideInUp 0.8s ease-out 0.5s backwards;
    }
    
    .info-box strong {
      color: #1f2937;
      display: block;
      margin-bottom: 5px;
    }
    
    .footer {
      text-align: center;
      padding: 25px 20px;
      font-size: 12px;
      color: #9ca3af;
      border-top: 1px solid #e5e7eb;
      background: #f9fafb;
    }
    
    .footer-content {
      margin-bottom: 10px;
    }
    
    .highlight {
      color: #e63946;
      font-weight: 600;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="header-content">
        <h1>
          Confirmed! 
          <svg class="checkmark" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
            <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
            <polyline points="22 4 12 14.01 9 11.01"></polyline>
          </svg>
        </h1>
        <p class="header-subtitle">Your trainer session is locked in</p>
      </div>
    </div>

    <div class="content">
      <p class="greeting">Hi <%= @booking[:user_name] %>,</p>

      <p class="intro-text">
        Your trainer booking has been <span class="highlight">successfully confirmed</span>. Get ready for an amazing training session at FitElite!
      </p>

      <div class="detail-card">
        <h3>
          <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
            <circle cx="12" cy="7" r="4"></circle>
          </svg>
          Trainer
        </h3>
        <div class="detail-row">
          <span class="detail-label">Name</span>
          <span class="detail-value"><%= @booking[:trainer_name] %></span>
        </div>
      </div>

      <div class="detail-card">
        <h3>
          <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
            <line x1="16" y1="2" x2="16" y2="6"></line>
            <line x1="8" y1="2" x2="8" y2="6"></line>
            <line x1="3" y1="10" x2="21" y2="10"></line>
          </svg>
          Session Details
        </h3>
        <% if @booking[:preferred_date].present? %>
          <div class="detail-row">
            <span class="detail-label">Date</span>
            <span class="detail-value"><%= @booking[:preferred_date] %></span>
          </div>
        <% end %>
        <% if @booking[:preferred_time].present? %>
          <div class="detail-row">
            <span class="detail-label">Time</span>
            <span class="detail-value"><%= @booking[:preferred_time] %></span>
          </div>
        <% end %>
      </div>

      <div class="detail-card">
        <h3>
          <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path>
          </svg>
          Contact Info
        </h3>
        <div class="detail-row">
          <span class="detail-label">Name</span>
          <span class="detail-value"><%= @booking[:user_name] %></span>
        </div>
        <div class="detail-row">
          <span class="detail-label">Email</span>
          <span class="detail-value"><%= @booking[:user_email] %></span>
        </div>
        <% if @booking[:user_phone].present? %>
          <div class="detail-row">
            <span class="detail-label">Phone</span>
            <span class="detail-value"><%= @booking[:user_phone] %></span>
          </div>
        <% end %>
      </div>

      <% if @booking[:goals_message].present? %>
        <div class="detail-card">
          <h3>
            <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
            </svg>
            Your Goals
          </h3>
          <p style="color: #495057; font-size: 13px; margin: 0; line-height: 1.6;">
            "<%= @booking[:goals_message] %>"
          </p>
        </div>
      <% end %>

      <div class="info-box">
        <strong>✓ What's Next?</strong>
        Your trainer will reach out shortly to confirm the final details. Keep an eye on your inbox and phone. If you need to reschedule, contact us at least 24 hours in advance.
      </div>

      <div class="cta-section">
        <a href="https://fitelitegym.com" class="cta-button">View Dashboard</a>
      </div>

      <p style="text-align: center; font-size: 13px; color: #6b7280; margin-top: 20px;">
        We're excited to work with you! Let's transform your fitness journey. 💪
      </p>
    </div>

    <div class="footer">
      <div class="footer-content">
        <p style="margin: 5px 0; font-weight: 600;">FitElite Gym</p>
        <p style="margin: 5px 0;">📧 notifications@fitelitegym.com</p>
      </div>
      <p style="margin: 10px 0 0 0;">This is an automated notification. Please do not reply.</p>
    </div>
  </div>
</body>
</html>
