<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      line-height: 1.6;
      color: #333;
      background-color: #f5f5f5;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      background-color: white;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }
    .header {
      background: linear-gradient(135deg, #dc2626 0%, #991b1b 100%);
      color: white;
      padding: 40px 20px;
      text-align: center;
      animation: slideIn 0.5s ease-out;
    }
    .header h1 {
      margin: 0;
      font-size: 28px;
      font-weight: 700;
      letter-spacing: 0.5px;
    }
    .header p {
      margin: 10px 0 0 0;
      font-size: 14px;
      opacity: 0.95;
    }
    .content {
      padding: 30px 20px;
    }
    .alert-box {
      background-color: #fef2f2;
      border-left: 4px solid #dc2626;
      padding: 15px 20px;
      margin-bottom: 25px;
      border-radius: 4px;
    }
    .alert-box h3 {
      margin: 0 0 10px 0;
      color: #dc2626;
      font-size: 16px;
      font-weight: 600;
    }
    .alert-box p {
      margin: 0;
      color: #666;
      font-size: 14px;
    }
    .section {
      margin-bottom: 25px;
      padding: 20px;
      background-color: #f9f9f9;
      border-left: 4px solid #dc2626;
      border-radius: 4px;
      animation: fadeIn 0.6s ease-out;
    }
    .section h3 {
      margin: 0 0 15px 0;
      color: #dc2626;
      font-size: 16px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    .section-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 15px;
    }
    .info-item {
      margin-bottom: 12px;
    }
    .info-label {
      font-size: 12px;
      color: #888;
      text-transform: uppercase;
      font-weight: 600;
      letter-spacing: 0.5px;
      margin-bottom: 3px;
    }
    .info-value {
      font-size: 16px;
      font-weight: 500;
      color: #333;
    }
    .cancelled-badge {
      display: inline-block;
      background-color: #dc2626;
      color: white;
      padding: 6px 12px;
      border-radius: 4px;
      font-size: 12px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    .footer {
      background-color: #f9f9f9;
      border-top: 1px solid #e0e0e0;
      padding: 20px;
      text-align: center;
      font-size: 12px;
      color: #888;
    }
    .divider {
      border: 0;
      height: 1px;
      background: #e0e0e0;
      margin: 20px 0;
    }
    @keyframes slideIn {
      from {
        transform: translateY(-10px);
        opacity: 0;
      }
      to {
        transform: translateY(0);
        opacity: 1;
      }
    }
    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }
    @media (max-width: 600px) {
      .section-grid {
        grid-template-columns: 1fr;
      }
      .header h1 {
        font-size: 22px;
      }
      .content {
        padding: 20px 15px;
      }
      .section {
        padding: 15px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Header -->
    <div class="header">
      <h1>❌ Booking Cancelled</h1>
      <p>A client has cancelled their training session</p>
    </div>

    <!-- Content -->
    <div class="content">
      <!-- Alert Box -->
      <div class="alert-box">
        <h3>⚠️ Cancellation Notice</h3>
        <p>
          <strong><%= @booking[:user_name] || "A client" %></strong> has cancelled their training session scheduled for 
          <strong><%= @booking[:preferred_date] %></strong> at <strong><%= @booking[:preferred_time] %></strong>.
        </p>
      </div>

      <!-- Cancelled Session Details -->
      <div class="section">
        <h3>📅 Cancelled Session Details</h3>
        <div style="text-align: center; margin-bottom: 15px;">
          <span class="cancelled-badge">Cancelled</span>
        </div>
        <div class="section-grid">
          <div class="info-item">
            <div class="info-label">Date</div>
            <div class="info-value"><%= @booking[:preferred_date] || "N/A" %></div>
          </div>
          <div class="info-item">
            <div class="info-label">Time</div>
            <div class="info-value"><%= @booking[:preferred_time] || "N/A" %></div>
          </div>
        </div>
      </div>

      <!-- Client Information -->
      <div class="section">
        <h3>👤 Client Information</h3>
        <div class="section-grid">
          <div class="info-item">
            <div class="info-label">Client Name</div>
            <div class="info-value"><%= @booking[:user_name] || "N/A" %></div>
          </div>
          <div class="info-item">
            <div class="info-label">Email</div>
            <div class="info-value">
              <a href="mailto:<%= @booking[:user_email] %>" style="color: #dc2626; text-decoration: none;">
                <%= @booking[:user_email] || "N/A" %>
              </a>
            </div>
          </div>
          <% if @booking[:user_phone].present? %>
            <div class="info-item">
              <div class="info-label">Phone Number</div>
              <div class="info-value">
                <a href="tel:<%= @booking[:user_phone] %>" style="color: #dc2626; text-decoration: none;">
                  <%= @booking[:user_phone] %>
                </a>
              </div>
            </div>
          <% end %>
        </div>
      </div>

      <hr class="divider">

      <!-- Additional Info -->
      <p style="text-align: center; color: #888; font-size: 13px; margin: 20px 0;">
        This time slot is now available for other bookings.
      </p>
    </div>

    <!-- Footer -->
    <div class="footer">
      <p style="margin: 0;">
        This is an automated notification from FitElite Gym<br>
        © <%= Date.today.year %> FitElite Gym. All rights reserved.
      </p>
    </div>
  </div>
</body>
</html>
