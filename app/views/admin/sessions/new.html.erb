<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Admin Login - FitElite Control Center</title>
  <%= csrf_meta_tags %>
  
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background: linear-gradient(135deg, #0f0f0f 0%, #1a1a2e 50%, #16213e 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }
    
    .login-container {
      width: 100%;
      max-width: 440px;
    }
    
    .login-card {
      background: rgba(17, 24, 39, 0.8);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 24px;
      padding: 48px;
      box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
    }
    
    .login-header {
      text-align: center;
      margin-bottom: 40px;
    }
    
    .logo {
      font-size: 42px;
      font-weight: 800;
      letter-spacing: -2px;
      color: white;
      margin-bottom: 8px;
    }
    
    .logo span {
      color: #ef4444;
    }
    
    .logo-subtitle {
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 4px;
      color: #6b7280;
    }
    
    .login-title {
      font-size: 24px;
      font-weight: 700;
      color: white;
      margin-top: 32px;
      margin-bottom: 8px;
    }
    
    .login-description {
      font-size: 14px;
      color: #9ca3af;
    }
    
    .flash {
      padding: 14px 18px;
      border-radius: 12px;
      margin-bottom: 24px;
      font-size: 14px;
      font-weight: 500;
    }
    
    .flash-alert {
      background: rgba(239, 68, 68, 0.1);
      border: 1px solid rgba(239, 68, 68, 0.3);
      color: #ef4444;
    }
    
    .flash-notice {
      background: rgba(34, 197, 94, 0.1);
      border: 1px solid rgba(34, 197, 94, 0.3);
      color: #22c55e;
    }
    
    .form-group {
      margin-bottom: 24px;
    }
    
    .form-label {
      display: block;
      font-size: 13px;
      font-weight: 600;
      color: #9ca3af;
      margin-bottom: 10px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    
    .form-input {
      width: 100%;
      padding: 16px 20px;
      background: rgba(0,0,0,0.4);
      border: 2px solid rgba(255,255,255,0.1);
      border-radius: 14px;
      color: white;
      font-size: 16px;
      transition: all 0.3s ease;
    }
    
    .form-input:focus {
      outline: none;
      border-color: #ef4444;
      box-shadow: 0 0 0 4px rgba(239, 68, 68, 0.15);
      background: rgba(0,0,0,0.5);
    }
    
    .form-input::placeholder {
      color: #6b7280;
    }
    
    .submit-btn {
      width: 100%;
      padding: 18px;
      background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
      border: none;
      border-radius: 14px;
      color: white;
      font-size: 16px;
      font-weight: 700;
      cursor: pointer;
      transition: all 0.3s ease;
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-top: 8px;
    }
    
    .submit-btn:hover {
      background: linear-gradient(135deg, #dc2626 0%, #b91c1c 100%);
      transform: translateY(-2px);
      box-shadow: 0 10px 30px rgba(239, 68, 68, 0.4);
    }
    
    .submit-btn:active {
      transform: translateY(0);
    }
    
    .login-footer {
      text-align: center;
      margin-top: 32px;
      padding-top: 24px;
      border-top: 1px solid rgba(255,255,255,0.05);
    }
    
    .login-footer p {
      font-size: 13px;
      color: #6b7280;
    }
    
    .security-badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      margin-top: 16px;
      padding: 8px 16px;
      background: rgba(34, 197, 94, 0.1);
      border-radius: 20px;
      font-size: 11px;
      color: #22c55e;
      text-transform: uppercase;
      letter-spacing: 1px;
    }
    
    .security-badge svg {
      width: 14px;
      height: 14px;
    }
    
    /* Animated background elements */
    .bg-decoration {
      position: fixed;
      border-radius: 50%;
      filter: blur(100px);
      opacity: 0.3;
      z-index: -1;
    }
    
    .bg-decoration-1 {
      width: 400px;
      height: 400px;
      background: #ef4444;
      top: -100px;
      right: -100px;
    }
    
    .bg-decoration-2 {
      width: 300px;
      height: 300px;
      background: #3b82f6;
      bottom: -50px;
      left: -50px;
    }
  </style>
</head>
<body>
  <div class="bg-decoration bg-decoration-1"></div>
  <div class="bg-decoration bg-decoration-2"></div>
  
  <div class="login-container">
    <div class="login-card">
      <div class="login-header">
        <div class="logo">FIT<span>ELITE</span></div>
        <div class="logo-subtitle">Control Center</div>
        <h1 class="login-title">Welcome Back</h1>
        <p class="login-description">Sign in to access the admin dashboard</p>
      </div>
      
      <% if flash[:alert] %>
        <div class="flash flash-alert"><%= flash[:alert] %></div>
      <% end %>
      <% if flash[:notice] %>
        <div class="flash flash-notice"><%= flash[:notice] %></div>
      <% end %>
      
      <%= form_tag admin_login_path, method: :post do %>
        <div class="form-group">
          <label class="form-label" for="email">Email Address</label>
          <%= email_field_tag :email, nil, class: "form-input", placeholder: "admin@fitelite.com", required: true, autofocus: true %>
        </div>
        
        <div class="form-group">
          <label class="form-label" for="password">Password</label>
          <%= password_field_tag :password, nil, class: "form-input", placeholder: "Enter your password", required: true %>
        </div>
        
        <button type="submit" class="submit-btn">Sign In</button>
      <% end %>
      
      <div class="login-footer">
        <p>Authorized personnel only</p>
        <div class="security-badge">
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path></svg>
          Secure Connection
        </div>
      </div>
    </div>
  </div>
</body>
</html>
